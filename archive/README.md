# FundGene Archive

This directory contains historical prototypes and reference material only.

Current rules:

- Do not use archive code as a mainline runtime entrypoint.
- Do not copy legacy UI, routing, AutoGen orchestration, MCP workbench code, or hard-coded local paths into the current product.
- Treat archived course material, scenario data, and historical analysis dimensions as reference assets that must be curated before reuse.
- Archived `node_modules` Markdown files are dependency noise and should not be treated as project documentation.
- Historical AutoGen and Vite prototype Markdown files have been removed to avoid being mistaken for current guidance.

Current project truth lives in:

- `../AGENTS.md`
- `../PROJECT_OVERVIEW.md`
- `../README.md`
